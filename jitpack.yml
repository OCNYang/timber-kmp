# JitPack Configuration for Timber KMP
# This file configures JitPack to build the Kotlin Multiplatform library

jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.8-tem || true
  - sdk use java 17.0.8-tem

install:
  - ./gradlew :timber:publishToMavenLocal -x test -x lint --stacktrace
